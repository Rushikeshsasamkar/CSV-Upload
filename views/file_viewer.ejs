<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="../css/file_viewer.css">
    <link rel="stylesheet" type="text/css" href="../css/header.css">
    <title>Your Page Title</title>
</head>
<body>
    <div class="nav-bar" style="position: fixed;">
        <a href="/" class="back-icon">
            <i class="fa-solid fa-circle-arrow-left" style="color: #333;"></i>
        </a>
        <div class="title">
            CSV Uploader
        </div>
        <div class="sub-title">
            Viewer
        </div>
        <div class="file-name">
            <%= fileName %>
        </div>
    </div>

    <div class="search-bar">
        <form id="searchForm">
            <div class="search-box">
                <input type="text" id="search-input" placeholder="Search data...">
                <button id="go" class="search-btn" type="button">Go</button>
            </div>
        </form>
        <div>
            <p id="total-rows">Total Rows: <%=length%></p>
        </div>
    </div>

    <div>
        <table>
            <thead>
                <tr>
                    <% for(let h of head) { %>
                        <th><%= h %></th>
                    <% } %>
                </tr>
            </thead>
            <tbody>
                <% for(let row of data) { %>
                    <tr>
                        <% for(let key of Object.keys(row)) { %>
                            <td><%= row[key] %></td>
                        <% } %>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>
